version: 2
updates:
  # This repo ships markdown + JSON manifests + shell install scripts and has no
  # application package manager in the repo root, so the only ecosystem to keep
  # current is the pinned GitHub Actions in .github/workflows.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
